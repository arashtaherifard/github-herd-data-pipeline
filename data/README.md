# Data

The original course project contains substantially larger raw, interim, processed, and task-specific modeling datasets.

Those generated datasets are intentionally omitted from the public portfolio repository.

This directory retains:

- the data dictionary;
- dataset summary metadata;
- Phase 3 dataset manifest;
- task-specific schema definitions.

The `collectors/` directory contains the GitHub collection scripts used to construct the underlying dataset.
